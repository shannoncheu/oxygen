# 验收记录

日期：2026-09-05。环境：Windows、Node.js 24.19.0、Next.js 16.3.4、Chrome 自动化浏览器；数据库集成使用 PGlite（PostgreSQL 引擎），生产配置使用 PostgreSQL 17。

## 本次增强的验证结果

- **汇率测试 12 项通过**：混合币种合计、JPY/KRW 零位精度、KWD 三位精度、交叉汇率、合计后舍入、手动覆盖、缺失汇率、溢出保护、快照校验、失败缓存和并发请求合并。
- **头像测试 3 项通过**：旧设置兼容、上传所有权、保存与重新读取、JSON 导出恢复、头像 ID 重映射和重置。
- **服务目录与图标测试 12 项通过**：32 项服务与别名、资源存在、App 名称匹配、官网图标解析、公网 URL 校验、DNS 固定与重定向、响应限额、SVG 清理及栅格化。
- 汇率接口实际请求通过，返回了全部 9 个支持币种。测试数据未写入源码或正式业务数据。
- TypeScript 检查通过。
- **完整单元与数据库测试 57 项通过**，其中包括上述增强测试以及既有计费、认证与备份测试。
- **浏览器回归 3 项通过**，生产构建通过。既有两套完整流程通过，新增订阅 Logo 上传流程单独复跑通过。

## 既有验证记录

- 已复现并修复头像保存的 PostgreSQL 数组绑定错误：旧 SQL 将图片 ID 数组展开为列表，执行时返回 `22P02 malformed array literal`。改为显式绑定参数后，空数组、单张、多张图片以及带特殊字符的值均能正确执行。本地事务现在也经过与生产一致的 Drizzle 参数编译路径。
- 新增上传存储失败、数据库写入失败后的文件清理、错误信息不泄露私有数据等回归；头像备份恢复测试仍通过。
- 375 px 手机视图实际点击「上传 Logo」打开文件选择器，上传图片后新建、刷新、更换及移除均正常。旧图加载失败时新上传图片仍能显示；不支持的格式及超过 2 MB 的文件不会发起上传，也不会替换原图。主动移除后不会在当前表单中被自动识别覆盖。

- 管理员 CLI 创建及重置实际运行通过，密码输入没有终端回显。
- 三个部署 / 备份 / 恢复脚本通过 Bash 5.2 的 `-n` 语法检查；脚本为 LF，无 BOM。

## 覆盖范围

| 范围 | 已验证的行为 |
| --- | --- |
| 私人访问 | 未登录页面转到登录；数据、操作、导入导出、图片和头像接口拒绝匿名访问 |
| 会话与安全 | CSRF 来源检查、登录限流、退出后旧会话失效、修改密码撤销其他会话、禁止再次创建管理员 |
| 持久化与同步 | 创建后刷新和重新登录仍存在；关闭再打开 PGlite 文件数据库；两个会话读取相同数据；并发修改一笔成功、一笔收到 409 |
| 图片与头像 | 真实像素解码、拒绝伪造格式、拒绝未经授权的图片引用；自定义头像保存、恢复、默认生成头像及备份重映射 |
| 在线图标 | 内网与保留地址、压缩 IPv6 本机地址、混合 DNS 响应、DNS 重绑定及重定向校验；响应体与头部限制；拒绝活跃 SVG、外部引用和压缩 SVG 绕过 |
| 日期 | 1 月 31 日 → 2 月末 → 3 月 31 日，闰年，跨年，每 N 月，自定义天 / 周，时区，试用转付费，暂停 / 恢复与停止续费 |
| 金额 | 原始金额使用最小单位整数；多币种按汇率合计；零位和三位精度；手动汇率优先；缺失或过期状态；年付仅计入扣费月；家庭分摊不重复计为支出 |
| 家庭组 | 包括组长的席位上限、成员复用、退出与过期释放席位、均摊余数、自定义金额与负责人余款 |
| 历史记录 | 价格 / 成员 / 头像修改保留旧快照；账单生成幂等；旧账单未确认不阻止未来计划；日期规则变化不追溯生成虚假账单 |
| 手动交费 | 本期分摊手动设置，金额总和校验，金额变化重新确认，成员档案与关系原子保存 |
| 数据迁移 | 完整 JSON 导出及带图片恢复，结构 / 版本 / 关联校验与确认覆盖；CSV 防公式注入；ICS 转义及稳定事件标识 |
| 响应式 | Chrome 375、390、768、1280、1440 px 下四个主要页面无横向溢出；390 px 底部导航不覆盖最后内容；浅色、深色及设置保存。375/390 px 触屏模拟下，添加订阅不自动聚焦输入框，文本输入控件至少 16 px，聚焦前后视口缩放保持 1，页面未禁止手动缩放 |

浏览器还实际操作了：登录、显示密码、新建家庭订阅、编辑席位、添加两名成员、满席禁止添加、本期均摊、记录成员交费、另一浏览器上下文读取，以及退出登录。

本次浏览器还验证了头像实际上传、刷新后侧栏与账号页显示、恢复默认头像、GPT/CLAUDED 搜索、搜索失败后改用官网、官网图标结果选取并保存、人民币和美元按手动汇率合计，以及手机弹窗焦点和缩放。设置页实际修改 USD 汇率从 7 到 8，总额从 240 元变为 260 元；清空汇率后显示缺失提示，恢复后再次正确合计。已查看手机弹窗、头像设置与主要页面截图；页面标题为 Oxygen，透明 SVG 和多尺寸 ICO 图标可访问，旧设置说明已移除。官网识别的浏览器验证使用固定响应，真实网络情况单独记录。

## 未运行与明确限制

- **未运行**真实服务器的 Docker 镜像构建、Compose 启动、PostgreSQL 17 容器恢复演练和真实域名 HTTPS 签发；本环境没有 Docker，也未获得你的服务器和域名配置。
- **未运行**iPhone / Android 实物键盘与 Safari 测试；响应式验收使用 Chrome 的视口模拟。
- **未完成真实网站图标抓取的联网验收**：本环境的部分公共域名经代理解析为 `198.18.0.0/15` 保留地址，公网校验按预期拒绝连接。没有为通过测试而放开内网地址限制；官网抓取和商店查找目前通过模拟响应验证。
- 服务器备份恢复目前经过代码审查和 Bash 语法检查，**不能将 JSON 恢复测试当作服务器备份恢复已实测**。
- 未实现邮件、推送、真实收款、第三方订阅自动同步和家庭成员登录。
- 演示内容只在明确运行的自动化测试数据库中生成。正式账号初始为空；测试数据库、会话、开发配置和依赖目录均不包含在源码压缩包里。

复现浏览器测试前先安装 Chromium：`pnpm exec playwright install chromium`。使用系统 Chrome 时可设置 `CHROME_PATH`。开始正式长期使用前，请在自己的服务器做一次部署与备份恢复演练。
